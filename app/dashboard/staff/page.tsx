import { DashboardShell } from "@/components/dashboard/dashboard-shell";

export default function StaffPage() {
  return (
    <DashboardShell
      eyebrow="Team management"
      title="Stylist scheduling, roles, and leave visibility"
      description="Use this module for employee setup, working hours, branch assignments, permissions, shift patterns, leave approvals, and limited operational access for hairdressers."
    />
  );
}

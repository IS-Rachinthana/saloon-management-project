import { DashboardShell } from "@/components/dashboard/dashboard-shell";

export default function CustomersPage() {
  return (
    <DashboardShell
      eyebrow="Customer CRM"
      title="Profiles, photos, preferences, and loyalty context"
      description="This space is ready for customer records, birthday campaigns, branch preferences, haircut inspiration uploads, service history, language settings, and targeted offers."
    />
  );
}

import { DashboardShell } from "@/components/dashboard/dashboard-shell";

export default function DashboardPage() {
  return (
    <DashboardShell
      eyebrow="Management overview"
      title="Salon operations in one responsive workspace."
      description="This starter dashboard is structured for reception, manager, stylist, customer, and super admin experiences. It gives you a clean shell for appointments, leave management, branch operations, CRM, notifications, and analytics."
    />
  );
}

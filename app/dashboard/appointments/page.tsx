import { DashboardShell } from "@/components/dashboard/dashboard-shell";

export default function AppointmentsPage() {
  return (
    <DashboardShell
      eyebrow="Appointments module"
      title="Booking engine foundation"
      description="Use this area for service selection, stylist availability, buffer logic, leave constraints, branch business hours, approval workflows, payment confirmation, and lock-based overlap prevention."
    />
  );
}
